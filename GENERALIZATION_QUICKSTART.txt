
================================================================================
                   GENERALIZATION EXPERIMENT - QUICK START
================================================================================

                              WORKFLOW OVERVIEW

    ┌─────────────────────────────────────────────────────────────────┐
    │ STEP 1: Generate Configurations                                 │
    │ ────────────────────────────────────────────────────────────────│
    │ Command: python generate_generalization_configs.py              │
    │ Output:  generalization_configs/ (10 JSON files)                │
    │ Time:    < 1 minute                                             │
    └─────────────────────────────────────────────────────────────────┘
                                    ↓
    ┌─────────────────────────────────────────────────────────────────┐
    │ STEP 2: Submit Jobs to HPC                                      │
    │ ────────────────────────────────────────────────────────────────│
    │ Command: bash hpc/submit_generalization_jobs.sh                 │
    │ Jobs:    50 (10 configs × 5 seeds)                              │
    │ Time:    ~12-24 hours (depending on queue)                      │
    │ Monitor: squeue -u $USER                                        │
    └─────────────────────────────────────────────────────────────────┘
                                    ↓
    ┌─────────────────────────────────────────────────────────────────┐
    │ STEP 3: Aggregate and Analyze Results                           │
    │ ────────────────────────────────────────────────────────────────│
    │ Command: python aggregate_generalization_analysis.py            │
    │ Output:  final_report_generalization/                           │
    │ Time:    ~5-10 minutes                                          │
    └─────────────────────────────────────────────────────────────────┘

================================================================================
                            EXPERIMENTAL DESIGN
================================================================================

TARGET PROTEINS:
  ✓ TyrosineProteinKinaseABL1_P00519       [Hyperparameter tuning - baseline]
  ✓ PyruvateKinaseM2_P14618                [Generalization test - same class]
  ✓ IsocitrateDehydrogenaseNADP_O75874     [Generalization test - diff class]

METHODS TESTED (with reasonable defaults):
  ✓ PCA Projection
  ✓ PCA Co-embedding
  ✓ t-SNE (perplexity=100)
  ✓ UMAP-Euclidean Projection (n_neighbors=100, min_dist=0.1)
  ✓ UMAP-Euclidean Co-embedding (n_neighbors=100, min_dist=0.1)

REPLICATES: 5 random seeds (42-46) per configuration

TOTAL EXPERIMENTS: 50 (vs. 3,600 in hyperparameter sweep = 98.6% reduction)

================================================================================
                             KEY DIRECTORIES
================================================================================

INPUT:
  generalization_configs/                   → Configuration files
  experiment_workspace_rerun_hyperparam_sweep/  → ABL1 baseline data

OUTPUT:
  experiment_workspace_generalization/      → Raw experimental results
  final_report_generalization/              → Analysis and visualizations
  slurm_logs/                              → HPC job logs

================================================================================
                         FILES CREATED FOR YOU
================================================================================

Configuration & Execution:
  ✓ generate_generalization_configs.py     [Generate config files]
  ✓ hpc/ummbas_generalization_cpu.sh       [SLURM execution script]
  ✓ hpc/submit_generalization_jobs.sh      [Job submission script]
  
Analysis & Reporting:
  ✓ aggregate_generalization_analysis.py   [Results aggregation]
  
Utilities:
  ✓ run_generalization_experiment.sh       [Quick start wrapper]
  
Documentation:
  ✓ GENERALIZATION_EXPERIMENT_README.md    [Full documentation]
  ✓ GENERALIZATION_SETUP_COMPLETE.md       [Setup summary]

================================================================================
                         QUICK START COMMANDS
================================================================================

Option A - Step by Step:
  $ python generate_generalization_configs.py
  $ bash hpc/submit_generalization_jobs.sh
  [Wait for jobs to complete]
  $ python aggregate_generalization_analysis.py

Option B - Interactive:
  $ bash run_generalization_experiment.sh 1    # Generate configs
  $ bash run_generalization_experiment.sh 2    # Submit jobs
  [Wait for jobs to complete]
  $ bash run_generalization_experiment.sh 3    # Analyze

================================================================================
                            EXPECTED OUTPUTS
================================================================================

Visualizations:
  • Method comparison bar charts (EF@1%, ROC-AUC, PR-AUC)
  • Performance heatmaps (Method × Protein)
  • Consistency scatter plots (Performance vs. Variation)

Tables:
  • performance_summary.csv              → Mean ± std for each method/protein
  • method_rankings.csv                  → Rank within each protein
  • generalization_assessment.csv        → Performance vs. ABL1 baseline
  • master_generalization_metrics.csv    → All raw data

================================================================================
                         COMPUTATIONAL SAVINGS
================================================================================

  Hyperparameter Sweep    →    3,600 experiments    →    ~2 weeks
  Generalization Test     →       50 experiments    →    ~1 day
  ────────────────────────────────────────────────────────────────
  Reduction              →       98.6%             →    93% faster

================================================================================
                            INTERPRETATION
================================================================================

GOOD GENERALIZATION:
  ✓ Methods maintain ranking across proteins
  ✓ Performance drop < 20% from ABL1
  ✓ Low variation (CV < 30%)
  → Default hyperparameters work well, no retuning needed

POOR GENERALIZATION:
  ✗ Rankings change dramatically
  ✗ Performance drop > 50% from ABL1
  ✗ High variation (CV > 50%)
  → Protein-specific tuning may be necessary

================================================================================
                          READY TO START!
================================================================================

All scripts are executable and ready to run.

Next step: python generate_generalization_configs.py

For detailed instructions, see: GENERALIZATION_EXPERIMENT_README.md

================================================================================
